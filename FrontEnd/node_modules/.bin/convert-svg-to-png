#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../convert-svg-to-png/bin/convert-svg-to-png" "$@"
  ret=$?
else 
  node  "$basedir/../convert-svg-to-png/bin/convert-svg-to-png" "$@"
  ret=$?
fi
exit $ret
